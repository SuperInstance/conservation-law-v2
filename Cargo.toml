[package]
name = "conservation-law-v2"
version = "0.1.0"
edition = "2021"
description = "Conservation law verification — proving that Grand Pattern systems conserve their scalar vibe"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
